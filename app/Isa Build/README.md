Ce dossier archive des workflows et éléments de build mis de côté temporairement.

Le fichier `build.yml` vient de `.github/workflows/` et ne sera plus exécuté par GitHub Actions tant qu'il reste ici.

Contexte actuel :
- l'APK Android n'est pas prioritaire
- la cible active est le build/deploy web via Vercel
